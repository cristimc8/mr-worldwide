export const selectUserFilteredRegions = (state) =>
  state.filtersReducer.selectedRegions

export const selectUserFilteredPopulationRange = (state) =>
  state.filtersReducer.selectedPopulationRange

export const selectUserFilteredLanguages = (state) =>
  state.filtersReducer.selectedLanguages

export const selectUserFilteredCurrencies = (state) =>
  state.filtersReducer.selectedCurrencies

export const selectUserFilteredTimeZone = (state) =>
  state.filtersReducer.selectedTimeZone

export const selectUserFilteredState = (state) =>
  state.filtersReducer
